import 'package:flutter/material.dart';

// ─────────────────────────────────────────────────────────────────────────────
//  MAA TARA AUTOMOBILES — Centralized Design Tokens / App Colors
// ─────────────────────────────────────────────────────────────────────────────
class AppColors {
  // Backgrounds
  static const Color bg = Color(0xFF0A1628);
  static const Color card = Color(0xFF162336);
  static const Color inputFill = Color(0xFF0F1B2B);
  static const Color navbarBg = Color(0xFF0D1B2A);

  // Brand / Accents
  static const Color accent = Color(0xFFE8A020);
  static const Color gold = Color(0xFFC78B35);
  static const Color goldIndicator = Color(0xFFFFC107);

  // Typography & Neutrals
  static const Color white = Colors.white;
  static const Color darkText = Color(0xFF1E1E1E);
  static const Color muted = Color(0xFF8FAABB);
  static const Color navMuted = Color(0xFF5A7290);
  static const Color divider = Color(0xFF1E3048);

  // Status & Semantic Colors
  static const Color green = Color(0xFF2ECC71);
  static const Color red = Color(0xFFE53935);
  static const Color blue = Color(0xFF2196F3);
  static const Color amber = Color(0xFFFF9800);
}

// Public alias for AppColors
typedef C = AppColors;
